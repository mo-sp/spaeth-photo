// TODO (P3): Bild-Pipeline mit Sharp.
// Erzeugt aus content/photos/source/ (bzw. demo-content/ als Fallback) die
// Varianten in public/img/<slug>/ sowie photos.manifest.json inkl. LQIP,
// zod-validierten Metadaten, Hash-Cache und Orphan-Cleanup.
// Platzhalter, damit `pnpm build` bereits jetzt durchläuft.
export {}
