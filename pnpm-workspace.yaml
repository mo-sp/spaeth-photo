# Native postinstall scripts pnpm blocks by default. Both belong to the Nuxt
# and ESLint toolchains and have to set up their platform-specific binary, or
# the build fails.
allowBuilds:
  esbuild: true
  unrs-resolver: true
